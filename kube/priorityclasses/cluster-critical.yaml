apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: cluster-critical
value: 1000000
globalDefault: false
description: >-
  Critical workloads that must schedule on saraneth when the Pi is unavailable.
  These preempt cluster-low pods if resources are constrained.
