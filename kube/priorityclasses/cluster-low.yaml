apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: cluster-low
value: 100
globalDefault: true
description: >-
  Default priority for non-critical workloads. These remain Pending or get
  preempted on saraneth when resources are needed by cluster-critical pods.
