apiVersion: v1
kind: Service
metadata:
  name: grocy
  namespace: grocy
  annotations:
    tailscale.com/hostname: grocy
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: grocy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
