apiVersion: v1
kind: Service
metadata:
  name: vestibule
  namespace: claude-vestibule
spec:
  selector:
    app: vestibule
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: metrics
      port: 9090
      targetPort: metrics
