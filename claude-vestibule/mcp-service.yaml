apiVersion: v1
kind: Service
metadata:
  name: vestibule-mcp
  namespace: claude-vestibule
spec:
  selector:
    app: vestibule-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: metrics
      port: 9090
      targetPort: metrics
